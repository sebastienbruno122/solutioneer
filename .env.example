# Cloudflare Web Analytics beacon token (free, cookieless — see README "Analytics" section).
# Get one at dash.cloudflare.com -> Analytics & Logs -> Web Analytics -> Add a site.
# Leave unset locally: the beacon script is simply omitted when this is empty.
PUBLIC_CF_BEACON_TOKEN=
